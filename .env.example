# Base de datos PostgreSQL
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=sistema_puntos
DB_HOST=localhost
DB_PORT=5432

# Para Docker: el frontend debe apuntar al backend.
# Desarrollo local: http://localhost:4000
# Docker (mismo equipo): http://localhost:4000
# Docker (acceso desde otra máquina): http://TU_IP:4000
NEXT_PUBLIC_API_URL=http://localhost:4000
